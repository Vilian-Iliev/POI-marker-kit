import * as THREE from 'three';
import { config } from '../config';

export function objectIsInView(pov: THREE.Vector3, target: THREE.Vector3, fov: number): boolean {
    throw new Error("Function not implemented.");
}