import * as THREE from 'three';
import { config } from '../config';

export function normalizePOVData(euler: THREE.Euler): THREE.Vector3{
    throw new Error("Function not implemented.");
}

export function isInView(pov:THREE.Vector3, target:THREE.Vector3, fov:number): boolean {
        throw new Error("Function not implemented.");
}